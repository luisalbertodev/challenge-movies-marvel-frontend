import * as S from './NotFoundMoreData.sc';

const NotFoundMoreData = () => {
  return (
    <S.NotFoundMoreData>
      <h3>!Whoops!</h3>
      <p>No hay más datos disponibles para mostrar</p>
    </S.NotFoundMoreData>
  );
};

export default NotFoundMoreData;
